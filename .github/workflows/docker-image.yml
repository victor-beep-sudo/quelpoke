name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest
    steps:
      - name: Docker Login
        run: |
          cat {{ secrets.DOCKER_KEY_VICTOR }} | docker login -u _json_key_base64 --password-stdin https://europe-west9-docker.pkg.dev
      -
        name: Build and push
        uses: docker/build-push-action@v6
        with:
          push: true
          tags: user/app:latest
